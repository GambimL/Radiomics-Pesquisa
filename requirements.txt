numpy
pandas
scikit-learn
matplotlib
seaborn
Pillow
opencv-python
SimpleITK
tqdm

# pyradiomics não tem wheel para Python 3.11 no PyPI e falha ao compilar da
# source (precisa de numpy/cython/versioneer já presentes antes do build).
# Instalar separadamente se for necessário rodar a extração de features:
#   pip install numpy cython versioneer
#   pip install pyradiomics --no-build-isolation
# pyradiomics
